name: Android Build & Release

# 觸發條件：
# 1. 當推送到 main 分支時 (可選，測試用)
# 2. 當推送 tag (例如 v1.0, v2.0) 時 -> 自動發布 Release
# 3. 手動觸發 (workflow_dispatch)
on:
  push:
    tags:
      - 'v*'
  workflow_dispatch:

jobs:
  build:
    runs-on: ubuntu-latest
    permissions:
      contents: write

    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      - name: Set up JDK 17
        uses: actions/setup-java@v4
        with:
          java-version: '17'
          distribution: 'temurin'
          cache: gradle

      - name: Grant execute permission for gradlew
        run: chmod +x gradlew

      # 編譯 Release 版本的 APK
      - name: Build with Gradle
        run: ./gradlew assembleRelease

      # 簽名 APK (使用你設定的 Secrets)
      - name: Sign Android Release
        uses: ilharp/sign-android-release@v1
        id: sign_app
        with:
          # 參數名稱修改如下：
          releaseDir: app/build/outputs/apk/release  # 從 releaseDirectory 改為 releaseDir
          signingKey: ${{ secrets.SIGNING_KEY }}     # 從 signingKeyBase64 改為 signingKey
          keyAlias: ${{ secrets.ALIAS }}             # 從 alias 改為 keyAlias
          keyStorePassword: ${{ secrets.KEY_STORE_PASSWORD }}
          keyPassword: ${{ secrets.KEY_PASSWORD }}
          buildToolsVersion: 34.0.0

      # 自動發布到 GitHub Releases
     # 2. 【修改】改名時加上 Tag 版本號
      - name: Rename Signed APK
        run: |
          # github.ref_name 代表你的 Tag (例如 v1.0.0)
          # 結果會變成: PWS_Patcher_v1.0.0.apk
          mv "${{ steps.sign_app.outputs.signedFile }}" "app/build/outputs/apk/release/PWS_Patcher_${{ github.ref_name }}.apk"

      # 3. 【修改】上傳對應檔名的 APK
      - name: Create Release
        uses: softprops/action-gh-release@v1
        if: startsWith(github.ref, 'refs/tags/')
        with:
          # 這裡也要用變數去抓剛剛改好名字的檔案
          files: "app/build/outputs/apk/release/PWS_Patcher_${{ github.ref_name }}.apk"
          generate_release_notes: true
          draft: false
          prerelease: false
